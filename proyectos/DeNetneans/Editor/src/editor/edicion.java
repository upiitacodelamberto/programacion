/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package editor;

/**
 *
 * @author Quantum
 */
public class edicion {
    
    private void cursiva() {
        
     bloc obj=new bloc();   
     
     /*if(pes1.isShowing()==true){
     Font f = pes1.getFont(); 
     pes1.setFont(new Font(f.getFontName(),Font.ITALIC,f.getSize()));    
     }
     if(pes2.isShowing()==true){
     Font f = pes2.getFont(); 
     pes2.setFont(new Font(f.getFontName(),Font.ITALIC,f.getSize()));   
     } 
     if(pes3.isShowing()==true){
     Font f = pes3.getFont(); 
     pes3.setFont(new Font(f.getFontName(),Font.ITALIC,f.getSize()));       
     } */ 
}
    
    
    
}
